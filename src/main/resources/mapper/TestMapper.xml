<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.scheduler.repository.TestRepo">
    <select id="findALl" resultType="com.example.scheduler.domain.User">
        SELECT * FROM user;
    </select>
    <insert id="insertUser">
        INSERT INTO batch.user(name)
        VALUES(#{name})
    </insert>
</mapper>
